package edu.univalle.battleship.model;

/**
 * Represents the orientation of a ship on the board.
 * <p>
 * A ship can be placed either horizontally or vertically.
 */
public enum Orientation {
    /** Horizontal orientation (row fixed, column increases). */
    HORIZONTAL,

    /** Vertical orientation (column fixed, row increases). */
    VERTICAL
}
